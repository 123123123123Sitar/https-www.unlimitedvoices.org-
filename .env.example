# Supabase config — copy to .env.local and fill from your Supabase project.
# (Dashboard → Project Settings → API → Project URL and anon/public key)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
